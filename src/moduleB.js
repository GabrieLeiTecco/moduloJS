module.exports.obj = {
    coincidencia(num, numAlea){
        if (num == numAlea) {
            console.log(`Parabéns!\n num = ${num}, aleatorio = ${numAlea}`)
        } else {
            console.log('Não foi dessa vez....')
        }
    }
}